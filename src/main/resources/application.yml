server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/eticaretcasedb
    username: postgres
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
productredisservice:
  redis:
    host: localhost
    port: 6379
